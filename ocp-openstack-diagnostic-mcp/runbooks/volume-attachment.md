# Volume Attachment Failure

Use `cinder.diagnose_attachment`, `cinder.get_volume`, and `cinder.get_pools`.

For PowerFlex, also run `cinder.diagnose_powerflex` and check linked Kubernetes Cinder pods.

