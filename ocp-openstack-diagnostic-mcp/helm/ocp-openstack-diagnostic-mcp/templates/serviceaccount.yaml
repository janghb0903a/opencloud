{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "ocp-openstack-diagnostic-mcp.serviceAccountName" . }}
  labels:
    {{- include "ocp-openstack-diagnostic-mcp.labels" . | nindent 4 }}
{{- end }}

